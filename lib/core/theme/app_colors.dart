import 'package:flutter/material.dart';

abstract final class AppColors {
  // Dark
  static const darkBackground = Color(0xFF17191E);
  static const darkSurface = Color(0xFF1D2026);

  static const darkShadow = Color(0xFF0D0F12);
  static const darkHighlight = Color(0xFF292D35);

  static const darkTextPrimary = Color(0xFFF5F6FA);
  static const darkTextSecondary = Color(0xFFA8ADB8);

  // Light
  static const lightBackground = Color(0xFFE9EDF2);
  static const lightSurface = Color(0xFFE9EDF2);

  static const lightShadow = Color(0xFFBEC3C9);
  static const lightHighlight = Color(0xFFFFFFFF);

  static const lightTextPrimary = Color(0xFF20232A);
  static const lightTextSecondary = Color(0xFF6C727D);

  // Shared
  static const primary = Color(0xFF7C9CFF);

  static const success = Color(0xFF67C99A);
  static const warning = Color(0xFFF2B96D);
  static const danger = Color(0xFFE77A7A);
}